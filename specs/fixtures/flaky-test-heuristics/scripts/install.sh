#!/bin/sh
# Import-fixture payload — DELIBERATELY present and deliberately inert.
#
# Real community skills ship helper scripts next to SKILL.md. DevDigest's
# importer must list this file as skipped and never unpack, store or execute it.
# If this line ever runs, the import path is broken.
echo "this script must never be executed by DevDigest"
